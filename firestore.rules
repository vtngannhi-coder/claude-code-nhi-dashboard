rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For development with emulator: allow all reads/writes
    // In production: replace with proper auth-based rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
